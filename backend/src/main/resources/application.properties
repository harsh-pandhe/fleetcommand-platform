spring.application.name=backend
spring.profiles.active=dev

app.jwt.secret=${JWT_SECRET:development-only-jwt-secret-must-be-at-least-32-characters}
app.jwt.expiration-seconds=${JWT_EXPIRATION_SECONDS:3600}

