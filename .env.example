# Local database connection used by the backend dev profile.
# Copy to .env and replace values if your local PostgreSQL setup differs.
DB_URL=jdbc:postgresql://localhost:5432/fleetcommand
DB_USERNAME=fleetcommand
DB_PASSWORD=change-me
